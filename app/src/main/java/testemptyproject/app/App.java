/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package testemptyproject.app;

import testemptyproject.list.LinkedList;

import static testemptyproject.utilities.StringUtils.join;
import static testemptyproject.utilities.StringUtils.split;
import static testemptyproject.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
